# Warmpact OpenAPI source of truth lives in packages/openapi-core/src/
# One YAML file per domain (plus common/). Do not edit this stub.
#
# Domains: identity, customers, underwriting, guarantees, pricing,
# forecasting, network, diagnostics, settlement, advisory, governance
#
# See packages/openapi-core/README.md
