/**
 * ExecuteRegisterBuildingConnection - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: customers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRegisterBuildingConnection Use Case
 *
 * DDD: Application use case for create Connection.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RegisterBuildingConnectionInput, RegisterBuildingConnectionOutput } from "../dto/connection.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { ConnectionClient } from "../ports";
import { ValidationError } from "../errors";

export class ExecuteRegisterBuildingConnection {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly connection: ConnectionClient
  ) {}

  async execute(input: RegisterBuildingConnectionInput): Promise<RegisterBuildingConnectionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cstId();

    // All field names extracted from OpenAPI spec: address, buildYear, customerId, heatedFloorAreaM2, networkZone
    if (!input.customerId) {
      throw new ValidationError("customerId is required");
    }
    if (!input.networkZone) {
      throw new ValidationError("networkZone is required");
    }
    if (!input.address) {
      throw new ValidationError("address is required");
    }

    const result = await this.connection.registerBuildingConnection({
      address: input.address,
      buildYear: input.buildYear,
      customerId: input.customerId,
      heatedFloorAreaM2: input.heatedFloorAreaM2,
      networkZone: input.networkZone,
    });

    return {
      // Response fields
    };
  }
}
