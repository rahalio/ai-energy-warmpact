/**
 * ExecuteGetSafetyFloors - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: guarantees
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetSafetyFloors Use Case
 *
 * DDD: Application use case for get SafetyFloor.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetSafetyFloorsInput, GetSafetyFloorsOutput } from "../dto/safety-floor.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { SafetyFloorRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteGetSafetyFloors {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly safetyFloor: SafetyFloorRepository
  ) {}

  async execute(input: GetSafetyFloorsInput): Promise<GetSafetyFloorsOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.grtId();

    // All field names extracted from OpenAPI spec: none
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.safetyFloor.getSafetyFloors({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as GetSafetyFloorsOutput;
  }
}
