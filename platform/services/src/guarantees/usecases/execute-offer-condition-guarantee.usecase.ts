/**
 * ExecuteOfferConditionGuarantee - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: guarantees
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOfferConditionGuarantee Use Case
 *
 * DDD: Application use case for get Guarantee.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OfferConditionGuaranteeInput, OfferConditionGuaranteeOutput } from "../dto/guarantee.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { GuaranteeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOfferConditionGuarantee {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly guarantee: GuaranteeRepository
  ) {}

  async execute(input: OfferConditionGuaranteeInput): Promise<OfferConditionGuaranteeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.grtId();

    // All field names extracted from OpenAPI spec: conditionProductId, connectionId, flatRatePriceId, serviceCreditRatePercent, termMonths
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).conditionProductId) {
      throw new ValidationError("conditionProductId is required");
    }
    const conditionproductId = (input as any).conditionProductId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.guarantee.offerConditionGuarantee({ conditionProductId: conditionproductId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${conditionproductId}`);
    }

    return entity as OfferConditionGuaranteeOutput;
  }
}
