/**
 * ExecuteCreateConditionProduct - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: guarantees
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCreateConditionProduct Use Case
 *
 * DDD: Application use case for create ConditionProduct.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CreateConditionProductInput, CreateConditionProductOutput } from "../dto/condition-product.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { ConditionProductRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteCreateConditionProduct {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly conditionProduct: ConditionProductRepository
  ) {}

  async execute(input: CreateConditionProductInput): Promise<CreateConditionProductOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.grtId();

    // All field names extracted from OpenAPI spec: exclusions, hotWaterAvailability, indoorTemperatureBandC, name, plainLanguageSummary, responseTimeHours
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.name) {
      throw new ValidationError("name is required");
    }
    if (!input.indoorTemperatureBandC) {
      throw new ValidationError("indoorTemperatureBandC is required");
    }
    if (!input.responseTimeHours) {
      throw new ValidationError("responseTimeHours is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (guarantees domain)
    const entityId = this.idGenerator.grtId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.conditionProduct.createConditionProduct({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as CreateConditionProductOutput;
  }
}
