/**
 * ExecuteIssueSupplyTemperatureSetpoint - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: network
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteIssueSupplyTemperatureSetpoint Use Case
 *
 * DDD: Application use case for get Setpoint.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { IssueSupplyTemperatureSetpointInput, IssueSupplyTemperatureSetpointOutput } from "../dto/setpoint.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { SetpointRepository } from "../ports";
import { NotFoundError } from "../errors";

export class ExecuteIssueSupplyTemperatureSetpoint {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly setpoint: SetpointRepository
  ) {}

  async execute(input: IssueSupplyTemperatureSetpointInput): Promise<IssueSupplyTemperatureSetpointOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.netId();

    // All field names extracted from OpenAPI spec: effectiveFrom, networkZone, rationale, targetSupplyTemperatureC
    // Load entity via repository (no ID field - uses input directly, pass correlation ID for response meta)
    const entity = await this.setpoint.issueSupplyTemperatureSetpoint({ ...input, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found`);
    }

    return entity as IssueSupplyTemperatureSetpointOutput;
  }
}
