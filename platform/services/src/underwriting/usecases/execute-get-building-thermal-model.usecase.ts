/**
 * ExecuteGetBuildingThermalModel - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: underwriting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetBuildingThermalModel Use Case
 *
 * DDD: Application use case for get ThermalModel.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetBuildingThermalModelInput, GetBuildingThermalModelOutput } from "../dto/thermal-model.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { ThermalModelRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetBuildingThermalModel {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly thermalModel: ThermalModelRepository
  ) {}

  async execute(input: GetBuildingThermalModelInput): Promise<GetBuildingThermalModelOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.uwrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).connectionId) {
      throw new ValidationError("connectionId is required");
    }
    const connectionId = (input as any).connectionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.thermalModel.getBuildingThermalModel({ connectionId: connectionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${connectionId}`);
    }

    return entity as GetBuildingThermalModelOutput;
  }
}
