/**
 * ExecuteUnderwriteBuildingConnection - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: underwriting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUnderwriteBuildingConnection Use Case
 *
 * DDD: Application use case for get Underwriting.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UnderwriteBuildingConnectionInput, UnderwriteBuildingConnectionOutput } from "../dto/underwriting.dto";
import type { ExecutionContextService, IdGeneratorService } from "@warmpact/services/_shared/index.js";
import type { UnderwritingClient, UnderwritingRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteUnderwriteBuildingConnection {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly underwritingRepository: UnderwritingRepository,
    private readonly underwritingClient: UnderwritingClient
  ) {}

  async execute(input: UnderwriteBuildingConnectionInput): Promise<UnderwriteBuildingConnectionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.uwrId();

    // All field names extracted from OpenAPI spec: conditionProductId, envelopeConditionNotes, historyMonthsAvailable, surveyPerformed
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).connectionId) {
      throw new ValidationError("connectionId is required");
    }
    const connectionId = (input as any).connectionId;

    // Action endpoint — persist/assess via underwriting client port
    const entity = await this.underwritingClient.underwriteBuildingConnection({
      ...input,
      connectionId,
    } as UnderwriteBuildingConnectionInput);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${connectionId}`);
    }

    return entity as UnderwriteBuildingConnectionOutput;
  }
}
