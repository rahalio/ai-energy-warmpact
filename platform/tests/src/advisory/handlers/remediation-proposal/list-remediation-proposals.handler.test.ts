/**
 * ListRemediationProposals handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: advisory
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listRemediationProposals } from "@warmpact/api-server/domains/advisory/handlers";
import type { AdvisoryDDDDependencies } from "@warmpact/api-server/domains/advisory";
import { createRemediationProposal as createRemediationProposalEntity } from "@warmpact/factories/advisory";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@warmpact/tests/shared";
import { ADVISORY_USE_CASES } from "../__factories__/use-cases.js";

describe("ListRemediationProposals", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AdvisoryDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AdvisoryDDDDependencies>(
      ADVISORY_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createRemediationProposalEntity({ orgId }),
      createRemediationProposalEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListRemediationProposals, "execute").mockResolvedValue(useCaseResult);

    await listRemediationProposals(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListRemediationProposals.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
