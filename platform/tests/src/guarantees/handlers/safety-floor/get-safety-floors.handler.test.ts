/**
 * GetSafetyFloors handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: guarantees
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getSafetyFloors } from "@warmpact/api-server/domains/guarantees/handlers";
import type { GuaranteesDDDDependencies } from "@warmpact/api-server/domains/guarantees";
import { createSafetyFloor as createSafetyFloorEntity } from "@warmpact/factories/guarantees";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@warmpact/tests/shared";
import { GUARANTEES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetSafetyFloors", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: GuaranteesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<GuaranteesDDDDependencies>(
      GUARANTEES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity", async () => {
    const entity = createSafetyFloorEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetSafetyFloors, "execute").mockResolvedValue(useCaseResult);

    await getSafetyFloors(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetSafetyFloors.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
