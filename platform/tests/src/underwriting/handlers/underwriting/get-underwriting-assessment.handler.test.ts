/**
 * GetUnderwritingAssessment handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: underwriting
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getUnderwritingAssessment } from "@warmpact/api-server/domains/underwriting/handlers";
import type { UnderwritingDDDDependencies } from "@warmpact/api-server/domains/underwriting";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@warmpact/tests/shared";
import { UNDERWRITING_USE_CASES } from "../__factories__/use-cases.js";

describe("GetUnderwritingAssessment", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: UnderwritingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<UnderwritingDDDDependencies>(
      UNDERWRITING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetUnderwritingAssessment, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { connectionId: entity.id };

    await getUnderwritingAssessment(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetUnderwritingAssessment.execute).toHaveBeenCalledWith(expect.objectContaining({ connectionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { connectionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetUnderwritingAssessment, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getUnderwritingAssessment(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
