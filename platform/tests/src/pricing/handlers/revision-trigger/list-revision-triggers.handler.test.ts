/**
 * ListRevisionTriggers handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: pricing
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listRevisionTriggers } from "@warmpact/api-server/domains/pricing/handlers";
import type { PricingDDDDependencies } from "@warmpact/api-server/domains/pricing";
import { createRevisionTrigger as createRevisionTriggerEntity } from "@warmpact/factories/pricing";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@warmpact/tests/shared";
import { PRICING_USE_CASES } from "../__factories__/use-cases.js";

describe("ListRevisionTriggers", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: PricingDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<PricingDDDDependencies>(
      PRICING_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createRevisionTriggerEntity({ orgId }),
      createRevisionTriggerEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListRevisionTriggers, "execute").mockResolvedValue(useCaseResult);

    await listRevisionTriggers(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListRevisionTriggers.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
