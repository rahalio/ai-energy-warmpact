/**
 * FaultFinding Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: diagnostics
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { FaultFindingEntity } from "@warmpact/core/diagnostics/models/index.js";
import { diagnosticsSchemas } from "@warmpact/core/diagnostics";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "dia";

export function createFaultFinding(overrides: Partial<FaultFindingEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    confidence: overrides.confidence ?? faker.number.int(),
    connectionId: overrides.connectionId ?? faker.string.uuid(),
    faultType: overrides.faultType ?? "high_return_temperature",
    status: overrides.status ?? "detected",
    ...overrides,
  };

  return diagnosticsSchemas.FaultFinding.parse(raw);
}

export function faultFindingBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createFaultFinding(overrides));
}
